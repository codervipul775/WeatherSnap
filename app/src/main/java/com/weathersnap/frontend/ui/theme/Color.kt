package com.weathersnap.frontend.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette (Yellow-Green from UI screenshots) ──
val Primary = Color(0xFFC8CC8A)
val PrimaryDark = Color(0xFFAAB06A)
val OnPrimary = Color(0xFF1A1D12)

// ── Background / Surface (Dark olive tones) ──
val Background = Color(0xFF1A1D12)
val Surface = Color(0xFF2D3124)
val SurfaceVariant = Color(0xFF3A3F2E)
val SurfaceHigh = Color(0xFF454A38)

// ── Text Colors ──
val OnBackground = Color(0xFFE8E8E0)
val OnSurface = Color(0xFFE8E8E0)
val OnSurfaceVariant = Color(0xFFB0B0A0)
val TextSecondary = Color(0xFF9A9A8A)

// ── Accent Colors (for weather detail chips) ──
val HumidityGreen = Color(0xFF66BB6A)
val WindBlue = Color(0xFF42A5F5)
val PressureYellow = Color(0xFFFFCA28)

// ── Status Colors ──
val ErrorRed = Color(0xFFEF5350)
val SuccessGreen = Color(0xFF66BB6A)

// ── Gradient Colors (top bar) ──
val GradientStart = Color(0xFFC8CC8A)
val GradientEnd = Color(0xFFA1CCBC)

// ── Header Specific Colors ──
val HeaderButtonBackground = Color(0xFF2E351A)
val GradientTextTitle = Color(0xFF1A1D12)
val GradientTextSubtitle = Color(0xFF455034)

// ── Image Size Chip Colors ──
val OriginalSizeBackground = Color(0xFF3A2E1E)
val OriginalSizeText = Color(0xFFFFB74D)
val CompressedSizeBackground = Color(0xFF1E3A2E)
val CompressedSizeText = Color(0xFF66BB6A)
